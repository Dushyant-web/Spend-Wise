# ── SpendWise Frontend Environment Variables ──────────────────────────────────
# Copy this file to .env.local for local development
# Set these as Environment Variables in your Netlify dashboard for production

# Your deployed FastAPI backend URL (no trailing slash)
# Local dev:
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
# Production (replace with your actual Render URL):
